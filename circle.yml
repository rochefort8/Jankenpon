machine:
    java:
        version: openjdk7

dependencies:
    override:
        - echo y | android update sdk --no-ui --filter "android-23, build-tools-23.0.1"
test:
    override:
        - ./gradlew test

deployment:
    develop:
        commands:
            - ./gradlew assembleDebug